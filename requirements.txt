# =============================================================================
# SmartBottle™ AI Vision SCADA - متطلبات ومكتبات المشروع (Requirements)
# =============================================================================
# يحتوي هذا الملف على كافة المكتبات البرمجية اللازمة لتشغيل النظام بالكامل.
# لتثبيت كافة المكتبات دفعة واحدة، نفذ الأمر التالي في الطرفية:
# pip install -r requirements.txt
# =============================================================================

# -----------------------------------------------------------------------------
# 1. مكتبات الذكاء الاصطناعي والرؤية الحاسوبية (AI & Computer Vision)
# -----------------------------------------------------------------------------
# ultralytics: مكتبة نماذج YOLOv8 / YOLO11 المسؤولة عن الاستدلال البصري واكتشاف العيوب بدقة وسرعة
ultralytics>=8.0.0

# opencv-python: معالجة الصور الرقمية، قراءة بث الفيديو من الكاميرات، ورسم مربعات التحديد
opencv-python>=4.8.0

# numpy: العمليات الحسابية المصفوفية السريعة لمعالجة مصفوفات بكسلات الصور
numpy>=1.24.0

# roboflow: إدارة وتحميل مجموعات البيانات المصنفة (Datasets) لتدريب نماذج الذكاء الاصطناعي
roboflow>=1.1.0

# -----------------------------------------------------------------------------
# 2. إطار عمل الويب والاتصال الفوري (Web Framework & Real-time Telemetry)
# -----------------------------------------------------------------------------
# Flask: إطار عمل بايثون الخفيف لبناء خادم الويب وواجهات برمجة التطبيقات (REST APIs)
Flask>=2.3.0

# Flask-SocketIO: دعم بروتوكول WebSockets لنقل بيانات الحساسات والتليمتري في الوقت الحقيقي (20Hz)
Flask-SocketIO>=5.3.0

# python-socketio: محرك معالجة مقابس الويب منخفض المستوى لضمان استقرار الاتصال اللحظي
python-socketio>=5.8.0

# -----------------------------------------------------------------------------
# 3. الاتصال بالعتاد الفيزيائي ومتحكم ESP32 (Hardware & Serial Interface)
# -----------------------------------------------------------------------------
# pyserial: إدارة الاتصال التسلسلي (UART / COM Port) بين الحاسوب ومتحكم ESP32
pyserial>=3.5
